    {{ form_start(form, {'attr': {'class': 'w-full max-w-lg'}}) }}

    <div class="my-4">
        <div class="my-custom-class-for-errors">
            {{ form_errors(form) }}
        </div>
    </div>

    <div class="mb-6">
        <div class="flex items-center">
            <div class="w-1/3">
                <label for="{{ form.title.vars.id }}" class="block text-gray-700 font-bold mb-2">
                    {{ form_label(form.title) }}
                </label>
            </div>
            <div class="w-2/3">
                {{ form_widget(form.title, {'attr': {'class': 'bg-gray-200 appearance-none border-2 border-gray-300 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-purple-500'}}) }}
                {{ form_errors(form.title) }}
            </div>
        </div>
    </div>

    <div class="mb-6">
        <div class="flex items-center">
            <div class="w-1/3">
                <label for="{{ form.shortDescription.vars.id }}" class="block text-gray-700 font-bold mb-2">
                    {{ form_label(form.shortDescription) }}
                </label>
            </div>
            <div class="w-2/3">
                {{ form_widget(form.shortDescription, {'attr': {'class': 'bg-gray-200 appearance-none border-2 border-gray-300 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-purple-500'}}) }}
                {{ form_errors(form.shortDescription) }}
            </div>
        </div>
    </div>

    <div class="mb-6">
        <div class="flex items-center">
            <div class="w-1/3">
                <label for="{{ form.longDescription.vars.id }}" class="block text-gray-700 font-bold mb-2">
                    {{ form_label(form.longDescription) }}
                </label>
            </div>
            <div class="w-2/3">
                {{ form_widget(form.longDescription, {'attr': {'class': 'bg-gray-200 appearance-none border-2 border-gray-300 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-purple-500'}}) }}
                {{ form_errors(form.longDescription) }}
            </div>
        </div>
    </div>

    <div class="mb-6">
        <div class="flex items-center">
            <div class="w-1/3">
                <label for="{{ form.releaseDate.vars.id }}" class="block text-gray-700 font-bold mb-2">
                    {{ form_label(form.releaseDate) }}
                </label>
            </div>
            <div class="w-2/3">
                {{ form_widget(form.releaseDate, {'attr': {'class': 'bg-gray-200 appearance-none border-2 border-gray-300 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-purple-500'}}) }}
                {{ form_errors(form.releaseDate) }}
            </div>
        </div>
    </div>

    <div class="mb-6">
        <div class="flex items-center">
            <div class="w-1/3">
                <label for="{{ form.coverImage.vars.id }}" class="block text-gray-700 font-bold mb-2">
                    {{ form_label(form.coverImage) }}
                </label>
            </div>
            <div class="w-2/3">
                {{ form_widget(form.coverImage, {'attr': {'class': 'bg-gray-200 appearance-none border-2 border-gray-300 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-purple-500'}}) }}
                {{ form_errors(form.coverImage) }}
            </div>
        </div>
    </div>

    <div class="mb-6">
        <div class="flex items-center">
            <div class="w-1/3">
                <label for="{{ form.staff.vars.id }}" class="block text-gray-700 font-bold mb-2">
                    {{ form_label(form.staff) }}
                </label>
            </div>
            <div class="w-2/3">
                {{ form_widget(form.staff, {'attr': {'class': 'bg-gray-200 appearance-none border-2 border-gray-300 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-purple-500'}}) }}
                {{ form_errors(form.staff) }}
            </div>
        </div>
    </div>

    <div class="mb-6">
        <div class="flex items-center">
            <div class="w-1/3">
                <label for="{{ form.casting.vars.id }}" class="block text-gray-700 font-bold mb-2">
                    {{ form_label(form.casting) }}
                </label>
            </div>
            <div class="w-2/3">
                {{ form_widget(form.casting, {'attr': {'class': 'bg-gray-200 appearance-none border-2 border-gray-300 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-purple-500'}}) }}
                {{ form_errors(form.casting) }}
            </div>
        </div>
    </div>

    <div class="mb-6">
        <div class="flex items-center">
            <div class="w-1/3">
                <label for="{{ form.score.vars.id }}" class="block text-gray-700 font-bold mb-2">
                    {{ form_label(form.score) }}
                </label>
            </div>
            <div class="w-2/3">
                {{ form_widget(form.score, {'attr': {'class': 'bg-gray-200 appearance-none border-2 border-gray-300 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-purple-500'}}) }}
                {{ form_errors(form.score) }}
            </div>
        </div>
    </div>

    <div class="mb-6">
        <div class="flex items-center">
            <div class="w-1/3">
                <label for="{{ form.type.vars.id }}" class="block text-gray-700 font-bold mb-2">
                    {{ form_label(form.type) }}
                </label>
            </div>
            <div class="w-2/3">
                {{ form_widget(form.type, {'attr': {'class': 'bg-gray-200 appearance-none border-2 border-gray-300 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-purple-500'}}) }}
                {{ form_errors(form.type) }}
            </div>
        </div>
    </div>

    {% if form.type.vars.value == 'serie' %}
        <div class="mb-6">
            <div class="flex items-center">
                <div class="w-1/3">
                    <label for="{{ form.seasons.vars.id }}" class="block text-gray-700 font-bold mb-2">
                        {{ form_label(form.seasons) }}
                    </label>
                </div>
                <div class="w-2/3">
                    {{ form_widget(form.seasons, {'attr': {'class': 'bg-gray-200 appearance-none border-2 border-gray-300 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-purple-500'}}) }}
                    {{ form_errors(form.seasons) }}
                </div>
            </div>
        </div>

        <div class="mb-6">
            <div class="flex items-center">
                <div class="w-1/3">
                    <label for="{{ form.episodes.vars.id }}" class="block text-gray-700 font-bold mb-2">
                        {{ form_label(form.episodes) }}
                    </label>
                </div>
                <div class="w-2/3">
                    {{ form_widget(form.episodes, {'attr': {'class': 'bg-gray-200 appearance-none border-2 border-gray-300 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-purple-500'}}) }}
                    {{ form_errors(form.episodes) }}
                </div>
            </div>
        </div>
    {% endif %}

    <div class="flex justify-end mt-4">
        <button type="submit" class="bg-purple-500 hover:bg-purple-400 focus:outline-none focus:shadow-outline text-white font-bold py-2 px-4 rounded">
            {{ button_label|default('Ajouter') }}
        </button>
    </div>

    {{ form_end(form) }}
{% endblock %}
